version: 1
project:
  name: zoro-game-bot
services:
  - name: bot
    type: app
    environment:
      type: python
      version: "3.11"
    build:
      install_command: "pip install -r requirements.txt"
    run:
      command: python bot.py
    resources:
      instance_type: start